# pptx-preview

## Development Setup

To link this package locally for development:

```shell
npm link
```

Then in your project directory:

```shell
npm link pptx-preview
```

This creates a symlink so you can test local changes without publishing to npm.
